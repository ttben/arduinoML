package ArduinoTestLanguage.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class STATUS_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<STATUS> constants = ListSequence.fromList(STATUS.getConstants()).iterator();
    while (constants.hasNext()) {
      STATUS constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<STATUS> constants = ListSequence.fromList(STATUS.getConstants()).iterator();
    while (constants.hasNext()) {
      STATUS constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    STATUS constant = STATUS.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
